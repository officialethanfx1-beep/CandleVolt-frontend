import React from "react";
import ReactDOM from "react-dom/client";
import CandleVolt from "./CandleVolt.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CandleVolt />
  </React.StrictMode>
);
